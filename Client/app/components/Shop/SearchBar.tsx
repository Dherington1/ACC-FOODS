import React from 'react'

// probably change to client for search to work 

const SearchBar = () => {
  return (
    <input
        type="search"
        placeholder="Search Products"
        className='searchInput'
    />
  )
}

export default SearchBar