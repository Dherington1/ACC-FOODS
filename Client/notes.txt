routes go off folder is the app folder 
    - http://localhost:3000/users // is based off the folder users and loads page.tsx
    - http://localhost:3000/users/new // is the new folder in the users and loads new.tsx